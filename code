#zip a folder
#Create a Cell in jupyter
#Copy & Paste below code
!tar chvfz name_of_newfolder.tar.gz name_of_folder_uwantto_zip/*

#upload a folder in current path
#upload its zip file
#create a cell
import zipfile as zf
files = zf.ZipFile("weights.zip", 'r')
files.extractall()
files.close()

